-- NOT usable code just yet — just the shape
--[[PlayerState = {
	_v = 1, -- schema version
	dubloons = 0, -- soft currency
	stationId = nil, -- 1..8, which wall/podium they own
	upgrades = { -- tycoon purchases
		cannon1 = false,
		cannon2 = false,
	},
}
]]
